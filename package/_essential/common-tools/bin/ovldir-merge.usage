#!/bin/sh

ovldir-merge \
  --destination=/opt/keycloak \
  --link-override=/opt/keycloak.var \
  --override=/opt/keycloak.aux \
  --source=/opt/keycloak.dist \

ovldir-list \
  --override=/ciep.user \
  --source=/ciep.d \
