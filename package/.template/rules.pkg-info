#!/usr/bin/make -f

SUITE ?= stable
DEB_TSVER = $(shell date -d @$(SOURCE_DATE_EPOCH) '+%y%m%d.%H%M%S')

DEB_EPOCH ?=$(empty)
DEB_MAINTAINER = Konstantin Demin <rockdrilla@gmail.com>
DEB_CHANGELOG_DATE = $(shell date -R -d @$(SOURCE_DATE_EPOCH))
