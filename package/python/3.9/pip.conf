[global]
# this is NOT error
no-cache-dir = false
# for use with proxy
# trusted-host = localhost
# index = http://localhost:8081/repository/proxy_pypi/pypi/
# index-url = http://localhost:8081/repository/proxy_pypi/simple/
