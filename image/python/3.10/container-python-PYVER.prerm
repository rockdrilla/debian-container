#!/bin/sh

set -e

if [ "$1" != "upgrade" ]; then
	bin=/usr/bin

	for suffix in '' @{PYTHON_MAJOR_VERSION} ; do
		update-alternatives --remove \
		  python${suffix} \
		  ${bin}/python@{PYTHON_BASE_VERSION}
	done
fi

#DEBHELPER#

exit 0
